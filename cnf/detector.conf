# Defaults equivalent to the CLAS12 RICH MAPMT/MAROC scaler analysis.
# Override these values for a different crate, slot numbering, or detector layout.

max_slots = 32
max_fibers = 32
n_asics = 3
n_pixels = 64
setup_header_lines = 23

# Legacy ssptest absolute channel:
# abs = maroc + 64*asic + 192*fiber + 192*32*(slot - slot_base)
slot_base = 3

clock_hz = 125000000

default_threshold = 230
default_gain = 64
threshold_offset = 25
threshold_min = 100
threshold_max = 700

flat_rate_min = 400
flat_rate_max = 425
shoulder_range = 25

pedestal_mean_min = 150
pedestal_mean_max = 220
min_pedestal_channels_per_asic = 1
noisy_pedestal_rms = 4
hot_rate_cps = 10000
